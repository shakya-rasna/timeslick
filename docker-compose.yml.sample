version: "3"

services:
  dtt_recharger_app:
    image: dtt_recharger_app:prod-latest
    restart: always
    environment:
      - MIX_ENV=xxx
      - POOL_SIZE=xx
      - SECRET_KEY_BASE=xxxxxxxx
      - DATABASE_URL=xxxxxxxxxxx
    ports:
      - "4000:4000"
